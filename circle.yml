machine:
  python:
    version: 2.7.10
  environment:
    DJANGO_SETTINGS_MODULE: payment_system.settings
dependencies:
  override:
    - pip install -r requirements.txt
test:
  override:
    - python manage.py test
    